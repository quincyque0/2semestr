cmake_minimum_required(VERSION 3.10)
project(QuicksortAnalysis C)

set(CMAKE_C_STANDARD 11)

# Основная программа (если у вас есть main-функция в quicksort.c)
add_executable(quicksort_analysis src/quicksort.c)

# ИЛИ если main-функция в отдельном файле, создайте src/main.c
# add_executable(quicksort_analysis src/quicksort.c src/main.c)

# Тесты
add_subdirectory(tests)