find_package(cmocka REQUIRED)

add_executable(test_quicksort
    test_quicksort.c
    ../src/quicksort.c
)

target_link_libraries(test_quicksort
    cmocka
)

enable_testing()
add_test(NAME quicksort_tests COMMAND test_quicksort)